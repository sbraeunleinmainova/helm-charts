apiVersion: v1
kind: Service
metadata:
  name: {{ include "packet-import.fullname" . }}
  labels:
    {{- include "packet-import.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "packet-import.selectorLabels" . | nindent 4 }}
